/* 📌
    Operadores Aritméticos: 

    = Asignación -- Se utiliza para asignar un valor a una variable.
        -Ejemplo:
            let numero = 5;
    
    + Suma  -- Se utiliza para sumar dos valores.
        -Ejemplo: 
            const numero = 5;
            const numero2 = 10;
            const resultado = numero + numero2; // 15

    - Resta -- Se utiliza para restar dos valores
        -Ejemplo: 
            const numero = 5;
            const numero2 = 10;
            const resultado = numero - numero2; // -5

    * Multiplicación -- Se utiliza para multiplicar dos valores.
        -Ejemplo: 
            const numero = 5;
            const numero2 = 10;
            const resultado = numero * numero2; // 50

    / División  -- Se utiliza para dividir dos valores.
        -Ejemplo: 
            const numero = 5;
            const numero2 = 10;
            const resultado = numero / numero2; // 0.5

    % Módulo  -- Se utiliza para obtener el residuo de una división.
        -Ejemplo: 
            const numero = 10;
            const numero2 = 3;
            const resultado = numero % numero2; // 1

    ++ Incremento -- Se utiliza para aumentar en 1 el valor de una variable.
        -Ejemplo: 
            let numero = 5;
            numero++; // 6

    -- Decremento -- Se utiliza para disminuir en 1 el valor de una variable.
        -Ejemplo: 
            let numero = 5;
            numero--; // 4
    
    ----------------------------------------------------------------------------------

    Operadores de Asignación:
    +=  -- Se utiliza para sumar el valor de la derecha a la variable de la izquierda.
        -Ejemplo: 
            let numero = 5;
            numero += 3; // 8

    -=  -- Se utiliza para restar el valor de la derecha a la variable de la izquierda.
        -Ejemplo: 
            let numero = 5;
            numero -= 3; // 2
    *=  -- Se utiliza para multiplicar el valor de la derecha a la variable de la izquierda.
        -Ejemplo: 
            let numero = 5;
            numero *= 3; // 15
    /=  -- Se utiliza para dividir el valor de la derecha a la variable de la izquierda.    
        -Ejemplo: 
            let numero = 5;
            numero /= 3; // 1.6666666666666667
    %=  -- Se utiliza para obtener el residuo de una división.
        -Ejemplo: 
            let numero = 10;
            numero %= 3; // 1
    ----------------------------------------------------------------------------------
    operadores de comparación:
    ==  -- Se utiliza para comparar si dos valores son iguales.
        -Ejemplo: 
            const numero = 5;
            const numero2 = '5';
            numero == numero2; // true

    ===  -- Se utiliza para comparar si dos valores son iguales y del mismo tipo.
        -Ejemplo: 
            const numero = 5;
            const numero2 = '5';
            numero === numero2; // false

    !=  --  Se utiliza para comparar si dos valores son diferentes.
        -Ejemplo: 
            const numero = 5;
            const numero2 = 10;
            numero !== numero2; // true

    !==  -- Se utiliza para comparar si dos valores son diferentes y diferente tipo de dato.
        -Ejemplo: 
            const numero = 5;
            const numero2 = 10;
            numero !== numero2; // true

    >  -- Se utiliza para comparar si un valor es mayor que otro.
        -Ejemplo: 
            const numero = 5;
            const numero2 = 10;
            numero > numero2; // false      

    <  -- Se utiliza para comparar si un valor es menor que otro.
        -Ejemplo: 
            const numero = 5;
            const numero2 = 10;
            numero < numero2; // true
    >=  -- Se utiliza para comparar si un valor es mayor o igual que otro.  
        -Ejemplo: 
            const numero = 5;
            const numero2 = 10;
            numero >= numero2; // false
    <=  -- Se utiliza para comparar si un valor es menor o igual que otro.
        -Ejemplo: 
            const numero = 5;
            const numero2 = 10;
            numero <= numero2; // true

    ----------------------------------------------------------------------------------
    Operadores Lógicos:

    &&  -- Se utiliza para comparar si dos condiciones son verdaderas.
        -Ejemplo: 
            const numero = 5;
            const numero2 = 10;
            numero > 3 && numero2 < 15; // true

    ||  -- Se utiliza para comparar si una de las dos condiciones es verdadera.

        -Ejemplo:
            const numero = 5;
            const numero2 = 10;
            numero > 3 || numero2 < 5; // true
    
    !  -- Se utiliza para negar una condición.
        -Ejemplo: 
            const numero = 5;
            const numero2 = 10;
            !(numero > 3 && numero2 < 15); // false
*/
