/* 📌 Arreglos

Los arreglos son una estructura de datos que nos permite almacenar varios valores en una misma variable. Estos valores pueden ser de cualquier tipo y cada uno de ellos se almacena en una posición determinada del arreglo.

Para declarar un arreglo, utilizamos corchetes [] y separamos los elementos con comas.

Ejemplo: 
    const arreglo = [1, 'Hola', true, {nombre: 'Juan'}, [1, 2, 3]];

En este caso, el arreglo contiene un número, un string, un booleano, un objeto y otro arreglo.
    
Para acceder a un elemento de un arreglo, utilizamos su índice. Los índices en los arreglos comienzan en 0, por lo que el primer elemento tiene índice 0, el segundo tiene índice 1, y así sucesivamente.

Ejemplo: const numeros = [1, 2, 3, 4, 5];

Para acceder al primer elemento del arreglo, utilizamos la siguiente sintaxis:
    console.log(numeros[0]); // Imprime 1
    

*/
