/* 📌 templateStrings 

    const nombres = "Carlos";
    const edad = 30;
    const pais = "Colombia";

    console.log(
    "La persona es " + nombres + " tiene " + edad + " años y es de " + pais
    );

    console.log(`La persona es ${nombres} tiene ${edad} años y es de ${pais}`);

    Ambas formas son correctas, pero la segunda es más limpia y fácil de leer.
    Las comillas invertidas `` se utilizan para definir un template string.
*/
